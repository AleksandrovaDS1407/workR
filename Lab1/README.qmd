# Лабораторная работа 1
## Изучение базовых строительных блоков R

В этой работе я установила R и RStudio, а также пакет `swirl`.

### Ход работы:

1. Установка прошла успешно.
2. Пакет `swirl` был установлен с помощью команды `install.packages("swirl")`.
3. Задание было запущено через `swirl::swirl()`.
